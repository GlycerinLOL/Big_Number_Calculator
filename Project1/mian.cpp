#include <iostream>
#include "Calculator.h"
#include "Number.h"

int main() {

	/*
	Number x("123456789");
	Number y("3.1415926");

	cin >> x; // 块 123456789
	cin >> y; // 块 3.1415926

	cout << x + y << endl;
	cout << x - y << endl;
	cout << x * y << endl;
	cout << x / y << endl;

	//Integer x = "123 * 8 + 456";
	//Decimal y = "-1.0 / 3 - 45 / 13.0";

	//vector<NumberObject> nums;
	//nums.push_back(&x);
	//nums.push_back(&y);
	//for (const auto& num : nums)
	//cout << num << endl;


	//经Ω + 跑计
	-B ^ 0.5
	//计 + 经Ω
	(3.1415926 ^ 0.5) ^ (3 * 8.5)
	//计 + 顶
	(8.5 / 0.25)!
	*/

	return 0;
}